<div class="container">
    <h1>Public Posts</h1>
    {{#each posts}}
    <div class="media">
         <img id="commentUserImage" src="{{user.image}}" class="mr-3" alt="...">
            <div class="media-body">
                <h4 class="mt-0">{{user.fullName}}</h4>
                    <h4>{{title}}</h4>
                <p>{{body}}</p>
                <small>{{date}}</small>
                <
  </div>
</div>
<hr>
{{else}}
<p>User does not have any public posts</p>
{{/each}}
</div>